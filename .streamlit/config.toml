# Dark theme baseline — screen-share friendly (large, high-contrast).
[theme]
base = "dark"
primaryColor = "#4F8DFD"
backgroundColor = "#0B1120"
secondaryBackgroundColor = "#111827"
textColor = "#E2E8F0"
font = "sans serif"

[server]
headless = true
runOnSave = true
