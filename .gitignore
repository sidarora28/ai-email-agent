# --- Secrets / auth (NEVER commit) ---
.env
token.json
credentials.json

# --- All local data is PII / regenerable — keep it out of the repo ---
# (voice pairs, indexes, records, auth). Everything under data/ stays local.
data/*
!data/.gitkeep

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- OS ---
.DS_Store
Thumbs.db
