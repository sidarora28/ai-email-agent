# YOUR categories — fill these in LIVE on stage.
#
# These are not generic inbox labels. They're the buckets YOU actually triage by.
# Each entry needs a `name` and a one-line `description` of what belongs in it.
#
# Shape to follow (delete this and write your own during the demo):
#
# categories:
#   - name: your_category
#     description: what belongs here
#   - name: another_one
#     description: ...

categories: []
