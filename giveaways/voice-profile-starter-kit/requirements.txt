# Voice Profile Starter Kit — pinned for a reproducible run (Python 3.11+)

# --- Voice profile via embeddings (runs locally, no API key) ---
sentence-transformers==3.0.1   # local embedding model
chromadb==0.5.5                 # local persistent vector store

# --- Gmail extraction ---
google-api-python-client==2.140.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# --- misc ---
python-dotenv==1.0.1
